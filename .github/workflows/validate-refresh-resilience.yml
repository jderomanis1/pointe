name: Refresh Resilience Validation

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  reconnect-refresh:
    if: github.head_ref == 'codex/security-and-mobile-hardening'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run resilience browser tests
        run: pnpm exec playwright test e2e/reconnect-vacancy.spec.ts
