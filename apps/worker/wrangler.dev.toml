# S10.i.c2 — dev/CI wrangler config. Selected by the `dev:e2e` script
# (`wrangler dev -c wrangler.dev.toml --port 8787 --ip 127.0.0.1 --local`).
# The composed entry keeps the existing gated test routes and adds the
# production retrospective API/DO surface for browser-level validation.

name = "pointe-worker"
main = "src/workerWithRetro.dev.ts"
compatibility_date = "2026-01-01"
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

[[durable_objects.bindings]]
name = "RETRO"
class_name = "RetroRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]

[[migrations]]
tag = "v2"
new_sqlite_classes = ["RetroRoom"]

[[kv_namespaces]]
binding = "POINTE_SLUGS"
id = "kv_local_dev"

[vars]
POINTE_E2E_TOKEN = "dev-e2e-token"
RL_CREATE_PER_HOUR_OVERRIDE = "500"
